<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <gradient
        android:angle="180"
        android:endColor="@color/login_edit"
        android:startColor="@color/login_edit" />

    <stroke
        android:width="0px"
        android:color="@color/login_edit" />

    <solid android:color="@color/login_edit" />

    <corners
        android:bottomLeftRadius="8px"
        android:bottomRightRadius="8px"
        android:topLeftRadius="8px"
        android:topRightRadius="8px" />

</shape>